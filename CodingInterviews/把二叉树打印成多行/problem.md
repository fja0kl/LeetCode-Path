# 题目描述

从上到下按层打印二叉树，同一层结点从左至右输出。每一层输出一行。

# Solution

二叉树的层序遍历。

借助queue实现，有一个要求是：每一层一个数组保存当前层的数据；

实现方式：难度在于不确定当前层有多少个节点--在队列出栈之前，先记录queue长度，这个长度也就是当前层的节点数目；然后遍历当前层；遍历完之后，在重新进入while 队列循环。

中间变量，记录当前层数据，遍历完之后加入到最终结果中。

两层循环--内层循环是在层之间；外层循环确保换层。
